type Point {
    x = 0;
    y = 0;

    getX() => self.x;
    getY() => self.y;

    setX(x) => self.x := x;
    setY(y) => self.y := y;
}



function rec(n:number):number {
    if(n <= 1){
        print(n);
    }
    else{
        print(n);
        rec(n - 1);
    };
}

print("Enter a number: ");
